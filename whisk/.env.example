# Vercel AI Gateway — https://vercel.com/docs/ai-gateway
AI_GATEWAY_API_KEY=

# Neon (Vercel Storage → 프로젝트 연결 시 자동 주입)
# 로컬: cd whisk → vercel link → pnpm env:pull
# https://neon.tech/docs/guides/vercel-native-integration
DATABASE_URL=
# 마이그레이션·직접 TCP 등에 쓰는 비풀링 URL(통합이 넣어줄 때만 존재)
# DATABASE_URL_UNPOOLED=

# Vercel Postgres 템플릿 호환용 레거시 이름(없으면 무시)
# POSTGRES_URL=
